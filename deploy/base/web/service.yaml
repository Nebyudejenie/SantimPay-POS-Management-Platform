apiVersion: v1
kind: Service
metadata:
  name: posctl-web
  labels: { app.kubernetes.io/name: posctl-web }
spec:
  selector: { app.kubernetes.io/name: posctl-web }
  ports:
    - { name: http, port: 80, targetPort: http }
