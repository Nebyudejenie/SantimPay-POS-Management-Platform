/**
 * Published AI events (the module's public contract). Exposed as a {@code @NamedInterface} so
 * followup/tasks/notification can react to computed scores.
 */
@org.springframework.modulith.NamedInterface("events")
package com.santimpay.posctl.ai.events;
