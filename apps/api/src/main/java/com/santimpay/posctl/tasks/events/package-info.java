/**
 * Published task events (the module's public contract). Exposed as a {@code @NamedInterface}.
 */
@org.springframework.modulith.NamedInterface("events")
package com.santimpay.posctl.tasks.events;
