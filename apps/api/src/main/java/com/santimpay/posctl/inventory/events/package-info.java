/**
 * Published integration events of the inventory module (the module's public contract). Exposed as a
 * {@code @NamedInterface} so other modules may subscribe without touching inventory internals.
 */
@org.springframework.modulith.NamedInterface("events")
package com.santimpay.posctl.inventory.events;
